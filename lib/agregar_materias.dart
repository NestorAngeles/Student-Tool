import 'dart:io';
import 'dart:async';
import 'package:flutter/material.dart';

class agregar_materias extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Text(
          "Hola perra"
      ),
    );
  }
}